dependencies:
 pre:
  - bower install
 post:
  - npm install module-deps
  - npm install gulp -g

machine:
  environment:
    NODE_ENV: test
    CIRCLE_ENV: test
    DATABASE_USERNAME: ubuntu
    DATABASE_PASSWORD:
    DATABASE_URL: localhost
    DATABASE_NAME: circle_test
    DATABASE_DIALECT: mongodb
    DATABASE_URL: mongodb://localhost/circle_test
deployment:
  production:
    branch: master
    heroku:
      appname: mak-taba
  staging:
    branch: staging
    heroku:
      appname: maktaba-staging
